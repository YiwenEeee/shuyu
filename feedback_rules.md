# 书遇 · 推荐反馈语规则（matchScore 分档 · D3 定稿）

> 本文件定义「灵魂共振匹配」根据匹配度返回的不同反馈语策略，供 C（AI·数据）实现与 B/A 前端对齐。
> 需求变更：匹配只展示高分书友、按 `matchScore` 降序，因此档位只覆盖"已匹配上的人"，C 仅用于"列表为空"。
> 阈值全部集中在 `config` 中，D3 调优时只改数字，文案不在代码里硬编码。

## 分档总览

| matchScore | 档位 | 生成方式 | 说明 |
|---|---|---|---|
| ≥ 85 | S · 灵魂共振 | DeepSeek 个性化推荐语 | 最打动人，主推 |
| 70 ~ 85 | A · 高相关 | DeepSeek 个性化推荐语 | 语气稍收敛 |
| 55 ~ 70 | B · 较强相关 | 模板 + Top-N 原文 | 正向措辞，"你们在同频处相遇" |
| < 55 或列表为空 | C · 兜底（空态） | 固定模板 | 鼓励再上传；**仅列表为空时展示** |

> 说明：`match()` 已按 `matchScore >= 55` 过滤，低于 55 的候选不会出现在列表里，因此 C 不参与"逐条推荐语"，只在整页无候选时由 B/前端展示。

## 各档文案（示例）

### S · 灵魂共振（≥ 85）

由 DeepSeek 基于来源笔记 + 候选原文 + 匹配度生成，核心是"你们几乎说到一块去了"。

示例：

> 你写的『自由不是想做什么就做什么……』，和 TA 的这段几乎是同一句话。你们应该能聊很久。

### A · 高相关（70 ~ 85）

DeepSeek 生成，语气收敛，突出"同书同感、但角度不同"。

示例：

> 你们都读到了这本书里『孤独』的那一面，只是落笔角度不同。

### B · 较强相关（55 ~ 70）

用统一模板，附上 TA 的摘录，措辞正向（不再暗示"可能不太配"）。

示例：

> 你们读到了同一处共鸣，角度略有不同——说不定正好能互补。

### C · 兜底（< 55 或列表为空）

固定模板，引导用户多上传笔记。**只在没有任何匹配候选时展示。**

示例：

> 这次还没遇到真正同频的人。再上传一条笔记，命运的齿轮随时会转。

## 实现要点

- 只有 S、A 两档让 DeepSeek 自由发挥；B 用模板，避免低分时 AI 强行尬吹显得假。
- 档位基于 `matchScore`(0~100) 计算，**不是**余弦相似度——与前端显示的百分比保持一致。
- 阈值初始值：`strong=85`、`high=70`、`weak=55`（单位 matchScore），以真实数据校准。
- `match()` 只保留 `matchScore >= weak` 的候选，因此 C 只作为"空列表"提示，不参与逐条推荐。

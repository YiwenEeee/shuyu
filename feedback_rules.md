# 书遇 · 推荐反馈语规则（相似度分档）

> 本文件定义「灵魂共振匹配」根据相似度返回的不同反馈语策略，供 C（AI·数据）实现与 B/A 前端对齐。
> 阈值全部集中在 `config` 中，D3 调优时只改数字，文案不在代码里硬编码。

## 分档总览（共 4 档）

| 相似度 | 档位 | 生成方式 | 说明 |
|---|---|---|---|
| ≥ 0.70 | S · 灵魂共振 | DeepSeek 个性化推荐语 | 最打动人，主推 |
| 0.50 ~ 0.70 | A · 高相关 | DeepSeek 个性化推荐语 | 语气稍收敛 |
| 0.30 ~ 0.50 | B · 一般相关 | 模板 + Top-N 原文 | 提醒"可能只是同书不同见解" |
| < 0.30 或无结果 | C · 兜底 | 固定模板 | 鼓励再上传笔记 |

## 各档文案（示例）

### S · 灵魂共振（≥ 0.70）

由 DeepSeek 基于 Top-N 原文 + 相似度分数生成，核心是"你们几乎说到一块去了"。

示例：

> 你写的『自由不是想做什么就做什么……』，和 TA 的这段几乎是同一句话。你们应该能聊很久。

### A · 高相关（0.50 ~ 0.70）

DeepSeek 生成，语气收敛，突出"同书同感、但角度不同"。

示例：

> 你们都读到了这本书里『孤独』的那一面，只是落笔角度不同。

### B · 一般相关（0.30 ~ 0.50）

用统一模板，附上 Top-N 原文，提醒可能只是同书、但见解不同。

示例：

> 你们读的是同一本书，但各自抓住的点不太一样——说不定正好能互补。

### C · 兜底（< 0.30 或无结果）

固定模板，引导用户多上传笔记，不强行匹配。

示例：

> 这次还没遇到真正同频的人。再上传一条笔记，命运的齿轮随时会转。

## 实现要点

- 只有 S、A 两档让 DeepSeek 自由发挥；B、C 用模板兜底，避免低分时 AI 强行尬吹显得假。
- 百分比建议**不直接敞露原始分数**（如 0.52 用户没感觉），可做一次映射；或只展示档位文案。
- 阈值建议初始值：`strong=0.70`、`high=0.50`、`weak=0.30`，最终以真实数据校准（D3）。

